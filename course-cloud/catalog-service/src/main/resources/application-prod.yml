spring:
  datasource:
    url: jdbc:mysql://catalog-db:3306/catalog_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

logging:
  level:
    com.zjsu.gjh.catalog: INFO
    org.hibernate.SQL: WARN