spring:
  datasource:
    url: jdbc:mysql://enrollment-db:3306/enrollment_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

catalog-service:
  url: http://catalog-service:8081

logging:
  level:
    com.zjsu.gjh.enrollment: INFO
    org.hibernate.SQL: WARN